# draw square using Turtle
import turtle

board = turtle.Turtle()
 
for i in range(4):
	#change the number below for bigger square
    board.forward(70)
    board.right(90)
     
turtle.done()